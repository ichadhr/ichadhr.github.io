sudo: required
dist: trusty

language: ruby

rvm:
  - 2.2.3

nvm:
  - 4.1.2

before_script:
 - bash scripts/dep.sh

install:
 - bash scripts/dep-i.sh

script: gulp --gulpfile scripts/build.js

branches:
  only:
    - master